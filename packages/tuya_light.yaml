# Tuya light

light:
  - platform: tuya
    id: skyfandc_light

    name: "${light_name}"

    switch_datapoint: ${dp_light}
    dimmer_datapoint: ${dp_light_dimmer}

    min_value: 0
    max_value: 5

    restore_mode: ALWAYS_ON

    disabled_by_default: true